import {Link} from 'react-router-dom'; import {ChevronRight} from 'lucide-react'; import {formatCurrency} from '../../utils/currency';
export default function ExpenseList({group}){return <div className="list-card">{group.expenses.map(e=><Link className="expense-row" key={e.id} to={`/app/groups/${group.id}/expense/${e.id}`}><div className="expense-icon">{e.category==='Food'?'🍴':e.category==='Transport'?'🚕':e.category==='Stay'?'🏨':'🧾'}</div><div className="row-main"><b>{e.title}</b><span>{e.date} · {e.splitType}</span></div><strong>{formatCurrency(e.amount)}</strong><ChevronRight size={18}/></Link>)}</div>}
